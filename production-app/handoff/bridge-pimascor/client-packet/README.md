# Generated client packet

These PDFs are delivery snapshots generated from the Markdown sources under
`production-app/docs/`. They are not the source of truth and must not be used
as demo deployment instructions. Before issuing a new packet, regenerate the
documents after reconciling the current production requirements and the
separate demo index at `production-app/docs/DEMO-DOCUMENTATION-INDEX.md`.

The hosted service includes encrypted production backups. Admin and DCS can
review backup completion status in the application; restoration is not a web
feature and remains restricted to the named service owner through a
dry-run-first VPS CLI procedure.
